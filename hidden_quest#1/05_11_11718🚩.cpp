#include <bits/stdc++.h>
using namespace std;

string s;
int main () {
  while (true) {
    getline(cin, s); // 🚩 Caution: Newline(`\n`) ignored.
    if (s == "") break;
    cout << s << "\n";
  }
  return 0;
}